# ===================================
# GitHub デプロイ対応 .gitignore
# ===================================

# Dependencies (デプロイ時にnpm installで再インストールされる)
node_modules/

# Package manager logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables (機密情報を含む可能性があるため除外)
.env
.env.local
.env.development
.env.test
.env.production

# Test coverage reports (開発時のみ必要)
coverage/
*.lcov
.nyc_output

# Cache directories (開発時のキャッシュ)
.npm
.eslintcache
.cache
.parcel-cache

# Build outputs (必要に応じてビルドプロセスで生成)
dist/
build/
.next/
.nuxt/

# Logs (ランタイムログは除外)
logs/
*.log
pids/
*.pid
*.seed
*.pid.lock

# Temporary files
tmp/
temp/

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ===================================
# デプロイに必要なファイル（除外しない）
# ===================================
# package.json - 依存関係の定義
# package-lock.json - 依存関係のロック
# server.js - メインサーバーファイル
# public/ - 静的ファイル（HTML, CSS, JS）
# tests/ - テストファイル（CI/CDで使用）
# *.md - ドキュメント